/* Shared search dataset — one array covering every writeup, research entry, note,
   and project. The homepage sitewide search filters all of it; each section's local
   search pre-filters to its own type via the DOM instead of this array directly. */
const SITE_INDEX = [];
